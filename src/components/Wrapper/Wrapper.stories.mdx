import '../../app/globals.css';
import AvatarImage from '../AvatarImage/AvatarImage';
import {ArgTypes, Meta, Story} from '@storybook/addon-docs/blocks';
import { text, number, boolean, select } from '@storybook/addon-knobs';
import { CardWrapper, WrapperAnimationHoverButton } from './Wrapper';
import { PrimaryButton } from '../Button/Buttons';
import { action } from '@storybook/addon-actions';
import { iconOptions } from '../../Mock/Mock';

<Meta title="Wrapper Animation" component={AvatarImage} />

Avatar Image Animation

<Story name="Avatar Image" component={WrapperAnimationHoverButton}>
  <WrapperAnimationHoverButton>
    <AvatarImage
      image={text(
        'Image URL',
        'https://images.pexels.com/photos/6753871/pexels-photo-6753871.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2',
      )}
      height={number('Height', 200)}
      width={number('Width', 200)}
    />
  </WrapperAnimationHoverButton>
</Story>

Button Animation

<Story name="Button Animation" component={WrapperAnimationHoverButton}>
  <WrapperAnimationHoverButton>
    <PrimaryButton
      onClick={action('button-click')}
      disabled={boolean('Disabled', false)}
      icon={select('Icon', iconOptions, null)}
    >
      {text('Button Title', 'Hello world')}
    </PrimaryButton>
  </WrapperAnimationHoverButton>
</Story>
<ArgTypes of={WrapperAnimationHoverButton} />

Card Wrapper

<Story name="Card Wrapper" component={CardWrapper}>
  <CardWrapper>
    <div className="m-auto">
      <AvatarImage
        image={text(
          'Image URL',
          'https://images.pexels.com/photos/6753871/pexels-photo-6753871.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2',
        )}
        height={number('Height', 40)}
        width={number('Width', 40)}
      />
    </div>
    <div className="flex flex-col items-center justify-center">
      <h3>{text('Title', 'Title')}</h3>
    </div>
    </CardWrapper>
</Story>
<ArgTypes of={CardWrapper} />
