import '../../app/globals.css';
import {ArgTypes, Meta, Story} from '@storybook/addon-docs/blocks';
import { boolean, select } from '@storybook/addon-knobs';
import Switch from "./Switch";
import { themeOptions } from "../../Mock/Mock";
  import {
  action
} from "@storybook/addon-actions";

<Meta title="Switch" component={Switch} />

Switch
<Story name="Switch" component={Switch}>
  <Switch enabled={boolean('Checked', false)} iconChecked={select('Checked icon', themeOptions, themeOptions.Moon)} iconUnchecked={select('Unchecked icon', themeOptions, themeOptions.Sun)} onChange={action('Switch Toggled')} />
</Story>
<ArgTypes of={Switch} />
